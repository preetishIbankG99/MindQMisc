package innerclass;
//>Nested interface must be public if it is declared inside the interface but 
//>it can have any access modifier if declared within the class
//>Nested interfaces are declared static implicitly.
public interface NestedInterface1 {
void show();
public interface Message{
	void mssg();
}
}
