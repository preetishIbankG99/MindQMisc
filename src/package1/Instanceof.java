package package1;
//The java instanceof operator is used to test whether 
//the object is an instance of the specified type (class or subclass or interface)
public class Instanceof {

	public static void main(String[] args) {
		Instanceof f=new Instanceof();
		System.out.println(f instanceof Instanceof);//true

	}

}
